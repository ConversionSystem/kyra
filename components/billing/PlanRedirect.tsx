'use client';

/**
 * PlanRedirect — disabled during beta.
 * Stripe checkout is not active while the platform is free.
 * This component is kept as a no-op to avoid breaking imports.
 */
export function PlanRedirect() {
  return null;
}
